---
title: "SelectClause"
description: "Query clause for selecting specific schema fields to return in results"
---

## SelectClause

```python
SelectClause(schema_fields)
```

### Parameters

<ParamField path="schema_fields" type="Sequence[SchemaField]" required>
The schema fields to select for inclusion in query results.
</ParamField>

### Inheritance

**Inheritance Chain**: 
- `SelectClause` 
- → `QueryClause`
- → `ABC`

### Properties

<ParamField path="schema_fields" type="Sequence[SchemaField]">
The collection of schema fields to be returned in query results.
</ParamField>

### Static Methods

#### from_param()

```python
from_param(fields) -> SelectClause
```

<ParamField path="fields" type="SchemaField | Sequence[SchemaField]" required>
The field or fields to select.
</ParamField>

**Returns**: `SelectClause` - A new select clause instance.

### Methods

#### get_altered_knn_search_params()

```python
get_altered_knn_search_params(knn_search_clause_params) -> KNNSearchClauseParams
```

<ParamField path="knn_search_clause_params" type="KNNSearchClauseParams" required>
The KNN search parameters to modify with the selected fields.
</ParamField>

**Returns**: `KNNSearchClauseParams` - The modified search parameters with the selected fields applied.